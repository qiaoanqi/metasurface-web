# a-Si nG 高阶收敛探针（自洽口径）— 审稿储备结论

> 2026-08-13 生成。数据源：`data/ng_probe_selfconsistent_aSi.pkl`（20 个 roundtrip 闭环结构 × 5 阶）。
> 自洽口径（Qwen 定案）：以最高阶为参考测 ΔE，阶间两两 ΔE，不依赖已失传的旧 ng_probe 参考口径。
> 脚本：`_ng_probe_selfconsistent.py`（`--n-struct` 参数）。

## 1. 关键发现：grcwa 的 nG 参数语义（必须先讲清）

`rcwa_batch.py` 用 `grcwa` 的 `obj(nG_req, ...)` + `Init_Setup(Gmethod=1)`（parallelogramic 截断）。
grcwa 内部 `Gsel_parallelogramic` 把 nG **当作"谐波网格边长的平方"**：

```
NGroot = int(sqrt(nG)); 若偶数则减 1（保证奇数）
实际倒格矢网格 = NGroot × NGroot
```

实测（`grcwa.kbloch.Lattice_getG`, method=1）：

| 论文/代码写的 nG 参数 | 实际谐波数 | 每边谐波 |
|---|---|---|
| 65 | 49 | 7×7 |
| 101 | 81 | 9×9 |
| 121 / 131 / 151 | 121 | 11×11 |
| 169 / 201 | 169 | 13×13 |

**含义**：论文全文写的 "nG = 65 / 101" 实际是 **7×7=49 / 9×9=81 个倒格矢**，不是 65/101 阶。
131 与 151 结果完全相同，就是因为两者都映射到 11×11（这是此前"nG131 与 nG151 逐结构全同"
之谜的答案，不是收敛平台）。

## 2. 收敛曲线（自洽口径，以 13×13=169 为参考，n=20）

| nG 参数（实际网格） | mean ΔE | median | <1 JND | <2.3 JND |
|---|---|---|---|---|
| 65（7×7） | 4.41 | 4.11 | 5% | 30% |
| 101（9×9） | 2.23 | 1.70 | 30% | 65% |
| 131/151（11×11） | **0.85** | **0.62** | **80%** | **95%** |
| 201（13×13） | 0（参考） | — | — | — |

阶间两两 ΔE：7×7→9×9 = 2.56；9×9→11×11 = 1.58；11×11→13×13 = 0.85。

## 3. 收敛结论

- **a-Si（高对比强吸收）需要 11×11 谐波（121 个倒格矢，nG 参数 ≈121–151）才基本收敛**：
  mean ΔE 0.85，80% 结构 <1 JND、95% <2.3 JND。
- **7×7（nG=65）和 9×9（nG=101）明显不够**（ΔE 4.41 / 2.23）。
- 论文 §Solver convergence 的"nG 65→101 复验掉到 47%"其根因是：nG=101 实际只是 9×9，
  对 a-Si 的高对比 + 强吸收傅里叶展开仍不足；无损材料默认的 nG=65（7×7）已够，是因为
  它们对比低、收敛快。
- 论文写"nG=131 仍漂移"（旧参考口径）→ 自洽口径修正为"**11×11（nG≈131）已基本收敛**"，
  真正未收敛的是 7×7/9×9 两档。

## 4. 对论文的启示（审稿 Q&A 弹药，不动论文正文）

1. 若审稿人问"nG 收敛"：用上表回应，并说明 grcwa 的 nG 语义（7×7/9×9/11×11 网格）。
2. 若审稿人质疑"65/101 阶为何这么低"：说明是 grcwa 谐波网格边长平方语义，实际 49/81 谐波，
   属 grcwa 库约定（`Gsel_parallelogramic` 的 `int(sqrt(nG))` 规则）。
3. 潜在风险点（需作者知晓）：论文 Table 3 的 "nG65 / nG101" 列名若被审稿人按字面理解为
   "65/101 阶傅里叶谐波"，存在口径误解——正文已用 "nG = 65" 而非 "65 阶"，但未显式说明
   谐波网格语义。是否在终稿补一句 nG 语义注记，由作者/审计裁决（属"带病锁定"待收项）。
