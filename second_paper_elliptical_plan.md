# 第二篇：椭圆 × 双偏振一体化 — 四道前置门执行设计（v2，2026-08-13）

> 2026-08-13 起草（Hermes），v2 落实审计必改 5 条（2026-08-13 裁定）。
> 定案来源：Qwen 审计裁决（椭圆与偏振合并为单一项目；可证伪检验 =
> "诅咒框架在更高维形状空间是否成立"）；不启动批量生成，先过四道门。
> 审计已认可：§0 接入点分析、已写的 rcwa_batch.py 椭圆 diff（W_nm 穿线 +
> abs(W−D)<1e-12 路由回原圆形公式，L=W 按构造逐位一致，圆柱路径零回归）。

## 0. 现状澄清：椭圆原语的真实接入点（审计已认可，不动）

grcwa 的 `obj.Add_LayerGrid + Epsilon_fft` 接受**任意 eps 网格**——截面形状完全由
`rcwa_batch.py _rcwa_single_wl` 里的 mask 公式决定（L124-131）。

**已实现（代码审计已过）**：`rcwa_spectrum(..., W_nm=None)` 可选参数——
`W_nm=None` 走原圆形公式（圆柱路径零回归）；`W_nm` 给定走椭圆公式
`(X/(D/2))²+(Y/(W/2))² ≤ 1`；`abs(W_nm−D_nm) < 1e-12` 路由回圆形公式
（L=W 退化按构造逐位一致）。

**命名约定（审计必改 4，写死）**：
- `D_nm` ≡ 论文 2 的 **L**（x 向全轴，µm 下半轴 D/2）
- `W_nm` ≡ 论文 2 的 **W**（y 向全轴，µm 下半轴 W/2）
- 椭圆长宽比 `r = D/W ∈ [1, 3]`（r=1 即圆）
- θ_ellip（面内旋转）本期不做，记档

## 门 1：椭圆 × 双偏振 RCWA 管道构建

### 1a. 椭圆几何（已实现，审计已认可）
`rcwa_batch.py`：mask 公式替换 + `rcwa_spectrum` 签名扩展（D + W_nm 可选参数，
调用方零风险；`_process_sample` 不传 W_nm 走原路）。回归：圆柱池不受影响。

### 1b. 偏振参数（审计必改 2，门 1 新增工作项）
- grcwa 接口：`blk.MakeExcitationPlanewave(p_amp, p_phase, s_amp, s_phase)`，
  现调用 `(1, 0, 0, 0)` = 纯 p。**实现时先对照 grcwa 源码/文档确认 p/s 与 x/y 的
  对应关系**，然后 `rcwa_spectrum` 加 `pol ∈ {'TE', 'TM'}` 参数：
  - TE = 纯 s（或按 grcwa 确认的对应）、TM = 纯 p
  - 正入射约定：入射面取 x-z 面（phi 惯例），以 grcwa 源码为准
- **三条物理不变量（偏振版 L=W 门，写死判据）**：
  1. 圆 + 正入射：`spectrum(D, W=D, TE) ≡ spectrum(D, W=D, TM)` 逐位一致（旋转对称）
  2. 椭圆 L≠W：`TE ≠ TM`（必须分裂——若未分裂 = 偏振参数没生效，bug）
  3. 90° 旋转对称：`spectrum(L, W, TE) ≡ spectrum(W, L, TM)`（x/y 互换 + 偏振互换）
- 全部三条跑通才算门 1 过；结果入 gate pkl。

### 1c. 采样协议（审计必改 1：LHS → 均匀随机 + 拒绝法）
- **与论文 1 协议保持一致**：论文 1 的 `generate_params`（rcwa_batch.py L189-210）
  是均匀随机 + 拒绝法（tex 曾误称 LHS 已被审计抓出修正——relay #4 错类）。
  论文 2 换 LHS 会制造跨论文协议断裂，**不用 LHS**。
- 椭圆采样器 = `generate_params` 模式扩到 `(L, W, H, P)` 四维：
  `L, W ~ U[80, 350]`、`H ~ U[100, 600]`、`P ~ U[200, 600]`，
  拒绝条件（门 3 约束集，写死断言）：
  - `max(L, W) < P`
  - `f = π(L/2)(W/2)/P² ∈ [0.03, 0.70]`
  - `r = L/W ∈ [1, 3]`（L<W 时交换使 r≥1）

## 门 2：L=W→圆柱复现门（审计必改 3，判据收紧）

- **对比源写死**：TiO₂ 15 个 + a-Si 15 个——float eps 与 complex eps 是两条
  代码路径，都要过。结构取自**闭环存档**（closed_loop_*_roundtrip_N100.pkl，
  D/H/P 全可追溯），结构 ID（name 字段）记录进 gate pkl。
- **期望结果写死：逐位 0.0**——R/T 全谱 max abs diff 与 ΔE2000 都是 0.0。
  退化分支（abs(W−D)<1e-12）就是旧公式本身，路由正确则必然全零。
  **任何非零 = 路由或因子化 bug，停下查因**。
- 后备容差：`< 0.1` 仅在有书面归因时使用（如已知的网格/浮点环境差异），
  不默认可放松。
- 对比层级：**R/T 全谱**（81 波长逐点），不只是 RGB。
- 通过后 gate 脚本 + pkl + 结果 md 一并入库（生成脚本永留档，血统教训）。

## 门 3：约束口径统一（审计已认可，不动）

几何：`max(L,W) < P`；`L,W ∈ [80,350]nm`；H/P 同圆柱。
填充率：`f = π(L/2)(W/2)/P² ∈ [0.03, 0.70]`。长宽比：`r ∈ [1, 3]`。
全部写进采样函数 + 断言（违例拒绝）。

## 门 4：nG 收敛探针（审计必改 5 已落实）

- 20 个代表性椭圆结构 × nG {65, 101, 131, 201} × TE/TM；自洽口径
  （以最高阶为参考，沿用 nG 探针）；判据：选 "mean ΔE < 0.5 JND 的最小 nG 档" 为池 nG。
- 双偏振联合指标**预注册**：`max(ΔE_TE, ΔE_TM) < 2.3`（两偏振同时达标才算成功）。
- **耗时预算：一律用探针实测值，不用外推**（审计必改 5①：方案 v1 的
  "nG=65 单结构 ≈4s" 与存档实测 1.63s（LESSONS.md）不符，可能是 spawn 开销——
  探针本身跑 4 阶 × 2 偏振，各档耗时直接实测记录，池生成预算按探针实测外推）。
- **预算上报对象：记录并上报用户**（审计必改 5②：导师线已按用户指令无限期冻结，
  预算不构成阻塞门）。

## 交付物与里程碑

| 门 | 交付物 | 预估 |
|---|---|---|
| 1 | 椭圆管道（已写，审计已认可）+ 偏振参数 + 三条不变量 + 采样器 | 1 天 |
| 2 | 复现门（TiO₂15+a-Si15，逐位 0.0）+ gate pkl + 结果 md | 0.5 天 |
| 3 | 采样约束断言（随 1c 一起） | 0 |
| 4 | 探针脚本 + 实测耗时 + 结果归档 | 1-2 h 计算 + 0.5 天 |

门 1（偏振+采样）→ 门 2（复现）→ 门 4（探针）串行依赖；门 3 内嵌于门 1。
**审计放行后动代码**；门 2 通过前不 commit；论文文件与 data/*. 一个字节不碰。

## 风险与预案

- 椭圆网格锯齿（Nxy=256 对椭圆边界量化）：门 2 的逐位 0.0 判据会暴露；若锯齿主导，
  预案 Nxy=384 或解析边界细化（成本 ×2.25）。
- 双偏振求解成本：TE/TM 各全谱求解，池生成 ×2；预算爆炸预案 = 偏振交替采样
  （需审计裁决）。
- 椭圆收敛性：11×11 仍 <0.5 JND 不达标 → r 上限降 2 或池 nG 升 13×13（预算 ×6，上报用户）。
- 奇异矩阵（Singular，池生成已知数值问题）：沿用池的 retry_nG 链（nG, nG+50, nG+100）。

## 遗留（本期不做，记档）

- 椭圆面内旋转 θ_ellip（偏振复用的最终形态——双偏振双角度）
- 多层/FP 腔（fp_cavity.py 已探）
- 新材料填空（ZrO₂/Nb₂O₅ 等——每材料一次来源审计的成本纪律已立）

---

# 口径记录（审计 2026-08-14 裁定，写死防回归）

## 记录 1：质量过滤判据（论文 2 vs 论文 1 差异）

- **论文 1 真值**（train_rcwa.py L80-93）：无损质量过滤 = 池相对判据
  `|R+T − 衬底池均值| ≤ 0.05`
- **论文 2 实现**：绝对判据 `|R_plus_T_mean − 1.0| ≤ 0.05`（无损期望 mean=1.0）
- **审计裁决**：绝对判据成立，准予为论文 2 预注册口径。理由：池相对判据自指
  （整池系统性偏移 0.97 时会全部放行）；绝对判据直接对能量守恒真值负责，物理更优；
  且冒烟实测 nG=201 下 TiO₂ R+T=1.0000066，两判据实际重合。
- **可逆兜底**：每条记录已存档 `R_plus_T_mean` 原值——训练侧任何时候可重套任意判据，
  口径选择可逆。论文 2 Methods 须写明此差异及理由。

## 记录 2：采样器粒度（论文 2 vs 论文 1 差异）

- 论文 1：整数 nm（generate_params 的 uniform 采样后取整或本就整数域）
- 论文 2：连续浮点 nm（generate_params_elliptical 直接返回 float）
- 理由：连续采样对 ML 训练无害（输入归一化后连续/离散等价）；论文 2 Methods 注明即可。
- 不阻塞；已在池元数据 sampler_version 记录。
